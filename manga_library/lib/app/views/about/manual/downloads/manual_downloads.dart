import 'package:flutter/material.dart';

class ManualDownloadsPage extends StatelessWidget {
  const ManualDownloadsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [
        
      ],
    );
  }
}