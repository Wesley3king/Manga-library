import 'package:flutter/material.dart';

class ManualExtensionsPage extends StatelessWidget {
  const ManualExtensionsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [
        
      ],
    );
  }
}