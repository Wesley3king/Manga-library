import 'package:flutter/material.dart';

class ManualUpdatesPage extends StatelessWidget {
  const ManualUpdatesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [
        
      ],
    );
  }
}